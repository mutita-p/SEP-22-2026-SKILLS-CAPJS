{
  "configurations": [
    {
      "command": "cds watch --profile hybrid",
      "name": "CAP Debug Session",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ],
  "version": "0.2.0"
}